#command line codes
git clone https://github.com/zakanksha/Html-codes.git
git status
git checkout gh-pages
git status
#if changes made in files do:
git add .
git status
git commit -m "HTML codes"
git status
git push
git status


#to sync browser
browser-sync start --server --directory --files "*"
